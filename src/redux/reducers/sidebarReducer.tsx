let initialState = {}
type InitialStateType = typeof initialState

const sidebarReducer: any = (state = initialState, action: any):InitialStateType => {
    return state
}
export default sidebarReducer()